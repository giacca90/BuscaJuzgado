spring.application.name=back_spring

# Conexion a postgreSQL
# Configuración del datasource
spring.datasource.url=jdbc:postgresql://postgres:5432/buscajuzgado
spring.datasource.username=postgres
spring.datasource.password=giacca90
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de Hibernate y JPA
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración de la unidad de persistencia
spring.jpa.properties.jakarta.persistence.jdbc.url=jdbc:postgresql://postgres:5432/buscajuzgado
spring.jpa.properties.jakarta.persistence.jdbc.user=postgres
spring.jpa.properties.jakarta.persistence.jdbc.password=giacca90
spring.jpa.properties.jakarta.persistence.jdbc.driver=org.postgresql.Driver
spring.jpa.properties.hibernate.format_sql=true

# Configuración de SSL con archivos .pem
server.ssl.key-store-type=PEM
server.ssl.certificate=/certs/fullchain.pem
server.ssl.certificate-private-key=/certs/privkey.pem
